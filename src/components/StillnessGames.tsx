"use client";
import { useState } from "react";
import type { EmotionVector } from "../generation/universe";
import { audioGarden } from "../audio/AudioGarden";
export function StillnessGame({kind,emotion,sound}:{kind:"Echo Pool"|"Comet Trails";emotion:EmotionVector;sound:boolean}){const [marks,setMarks]=useState<{x:number;y:number}[]>([]);const touch=(e:React.PointerEvent<HTMLDivElement>)=>{const r=e.currentTarget.getBoundingClientRect(),mark={x:(e.clientX-r.left)/r.width*100,y:(e.clientY-r.top)/r.height*100};setMarks(v=>[...v.slice(-22),mark]);if(sound)audioGarden.note(marks.length,emotion)};const pool=kind==="Echo Pool";return <section className={`game stillness ${pool?"pool":"comets"}`}><div className="still-copy"><p className="game-kicker">{pool?"ECHO POOL":"COMET TRAILS"}</p><h2>{pool?"Touch the water.\nWatch it remember.":"Leave a little\nlight behind."}</h2><p>{pool?"Each touch sends a slow colour ripple across a night pool.":"Click or draw across the sky to make a temporary constellation trail."}</p></div><div className="play-surface" onPointerDown={touch} aria-label={pool?"Interactive echo pool":"Interactive comet sky"}>{marks.map((m,i)=><i key={i} className={pool?"ripple":"comet"} style={{left:`${m.x}%`,top:`${m.y}%`,animationDelay:`${i*-80}ms`}} />)}</div><button className="clear-game" onClick={()=>setMarks([])}>Begin again</button></section>}
